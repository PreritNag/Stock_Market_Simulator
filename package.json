{
  "name": "stock-market-simulator",
  "version": "1.0.0",
  "description": "Full-stack stock market simulator with virtual trading, real-time charts, and Razorpay integration",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "seed": "cd backend && node seeds/stockSeeder.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
